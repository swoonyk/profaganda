{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "echo 'Use: vercel dev' && exit 1",
    "start": "node dist/index.js",
    "clean": "rm -rf dist .vercel",
    "type-check": "tsc --noEmit",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0",
    "dotenv": "^17.2.2",
    "mongodb": "^6.3.0",
    "@google/generative-ai": "^0.7.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.8.2"
  }
}